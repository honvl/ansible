- name: Make sure we have localhost facts
  hosts: localhost

- name: Backup configurations
  hosts: all
  gather_facts: yes
  connection: network_cli
  tasks:
    - include_role:
        name: network-backup