- hosts: all
  collections:
    - paloaltonetworks.panos
  tasks:
  - name: Palo Alto check
    panos_check:
      username: '{{ username }}'
      password: '{{ password }}'
      ip_address: '{{ inventory_hostname }}'
